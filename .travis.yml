language: r
sudo: false
cache: packages
r_packages:
  - covr
before_script:
  - Rscript -e 'vaultr::vault_test_server_install()'
after_success:
  - Rscript -e 'covr::codecov()'
r_github_packages:
  - vimc/montagu-r
  - vimc/vaultr
env:
  global:
    - VAULTR_TEST_SERVER_INSTALL=true
    - VAULTR_TEST_SERVER_PORT=18200
    - VAULTR_BIN_PATH=.vault
addons:
  apt:
    packages:
      - libimage-exiftool-perl
